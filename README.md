# SigniQ
"SigniQ" — a monitoring system that detects attacks on digital signature infrastructure (nonce reuse, replay, anomalous signing patterns) and additionally flags which endpoints still use quantum-vulnerable signature schemes (RSA/ECDSA) so organizations know where to prioritize migration to post-quantum cryptography.
